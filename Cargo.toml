[package]
name = "locxml"
version = "0.1.0"
edition = "2024"

[dependencies]
chumsky = "0.12.0"
derive_more = { version = "2.1.1", features = ["deref", "from"] }
from_nested_tuple = "0.1.2"
itertools = "0.14.0"
