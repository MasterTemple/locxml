// XML declaration: <?xml version="1.0" encoding="UTF-8"?>
// To be implemented.
//
// The XML declaration is NOT a processing instruction (even though it looks
// like one). It must appear as the very first thing in the document and must
// not be counted as a node for XPath or EPUB CFI positioning.
